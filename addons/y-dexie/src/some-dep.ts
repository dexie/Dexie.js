import * as Y from 'yjs';

export const someDep = () => {
  // This function is a placeholder for some dependency logic.
  // It can be used to perform operations related to the y-dexie addon.
  console.log("someDep function called");
  Y.Array.from([]);
  return 1;
}
